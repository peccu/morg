# タスク管理

> 新規タスク追加・ステータス更新は `/task` スキルを使ってください。
> Lessons & Learnings は `.claude/rules/lessons.md` に記録します。

---

## 凡例

| ステータス | 意味 |
|-----------|------|
| 🔍 調査中 | 原因・方針を調査している |
| 📋 対応予定 | 方針決定済み、未着手 |
| 🚧 対応中 | 実装・修正中 |
| ✅ 完了 | 対応・確認済み |
| ⏸ 保留 | 優先度低・依存待ち |

---

## タスク一覧

### T001 · i18n対応

| 項目 | 内容 |
|------|------|
| **ステータス** | ✅ 完了 |
| **優先度** | 中 |
| **概要** | UI文字列の国際化対応（日本語・英語） |

**調査内容**
- 現状: 日本語ハードコード
- ライブラリ: vue-i18n
- 言語: 日本語・英語
- 言語選択: ブラウザ設定に自動追従 + UI切替ボタン + localStorage保存

**対応方針**
1. vue-i18n をインストール
2. `src/i18n/` に `ja.ts` / `en.ts` ロケールファイルを作成
3. `src/i18n/index.ts` で i18n インスタンスを生成（localStorage + ブラウザ検出）
4. `main.ts` に登録
5. 全コンポーネントの日本語文字列を `$t()` / `t()` に置き換え
6. InboxView のヘッダーメニューに言語切替ボタンを追加

**確認方法**
- ブラウザ言語設定を英語にして開くと英語UIが表示される
- UI切替で即時反映される
- 切替後リロードしても言語が維持される

---

### T002 · エラーコピーボタンのフィードバックなし

| 項目 | 内容 |
|------|------|
| **ステータス** | ✅ 完了 |
| **優先度** | 低 |
| **概要** | コピーボタン押下時に「コピーしました」等のフィードバックがない |

**調査内容**
- 対象ファイル: `apps/web/src/components/ThreadList.vue`, `apps/web/src/views/ThreadView.vue`
- `copyText()` が成功してもユーザーへの通知なし

**対応方針**
- `useToast()` の `show()` でトースト通知を出す
- または `copyText()` ヘルパー内でトースト呼び出し（共通化）

**確認方法**
- エラー表示状態でコピーボタンを押して「コピーしました」トーストが出ること

---

### T004 · ランディングページ（未ログイン時のトップ画面）

| 項目 | 内容 |
|------|------|
| **ステータス** | ✅ 完了 |
| **優先度** | 中 |
| **概要** | 未ログイン時のログイン画面を、アプリの説明・売りが伝わるランディングページに刷新する |

**背景**
- 現状の `/login` は Google ログインボタンだけのシンプルな画面
- 初めて訪れたユーザーが「何のアプリか」「使う価値があるか」を判断できない
- OSS として公開しているため、GitHubから来た人にも伝わる内容にしたい

**調査内容**
- 現状ファイル: `apps/web/src/views/LoginView.vue`
- 伝えたいこと（案）:
  - Gmail をスマホで快適に整理するためのPWAアプリ
  - 一括アーカイブ・削除・既読などの操作がモバイルで快適
  - プラグインで拡張可能（Webhook送信など）
  - OSSで自分でデプロイして使える

**対応方針**
- `LoginView.vue` をランディングページ兼ログイン画面として拡張
- 構成案: ヒーロー（キャッチコピー + Google ログインボタン）→ 機能紹介セクション → フッター（GitHub リンク）
- デザインは既存の forest カラーパレットに合わせる
- モバイルファーストで縦スクロール型

**確認方法**
- 未ログイン状態で `/login` を開き、アプリの概要・機能・ログインボタンが表示されること
- モバイル幅（375px）で崩れないこと

---

### T005 · プラグインの多言語対応

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 中 |
| **概要** | ビルトインプラグインのラベル・説明文が日本語ハードコードのため英語環境で未対応 |

**調査内容**
- 対象: `apps/web/src/plugins/` 配下のビルトインプラグイン（webhook, open-in-gmail, copy-subject 等）
- プラグインの `label`・`description`・`threadActions[].label` が文字列ハードコード
- PluginsView および ThreadView のアクションバーに表示されるため英語UIで日本語が混在する

**対応方針**
- プラグインの文字列を i18n キー参照に切り替えるか、`label` を関数 `() => t(...)` で返す形にする
- ロケールファイル（ja.ts / en.ts）に `plugins.*` 以下でビルトインプラグイン文字列を追加

**確認方法**
- 言語を EN に切り替えた状態でプラグイン一覧・スレッド詳細のアクションバーを開き、英語で表示されること

---

### T006 · ランディングページのPC・タブレット訴求改善

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 低 |
| **概要** | ランディングページの文言がスマホ・モバイル前提になっており、PC/タブレットユーザーへの訴求が弱い |

**調査内容**
- 現状の `landing.tagline` / `landing.desc` / 各機能説明がモバイル前提の表現
- 例: 「スマートフォン向けの Gmail 整理PWAアプリ」「ホーム画面に追加」など
- PCでも Gmail の一括処理・送信者フィルタ・プラグイン拡張などのメリットがある

**対応方針**
- タグライン・説明文をモバイル限定でない表現に更新（例: 「どのデバイスでも」「ブラウザから快適に」）
- 機能カードの説明もPCで使う場面を想定した記述に更新
- 英語・日本語両方のロケールファイルを修正

**確認方法**
- PC幅でランディングページを開き、PCユーザーが使いたいと思える文言になっていること
- モバイル幅（375px）でも崩れず自然な表現であること

---

### T007 · README 整備

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 中 |
| **概要** | リポジトリの README を整備し、アプリ概要・画面キャプチャ・セルフホスト手順・アーキテクチャを記載する |

**調査内容**
- 現状: `README.md` が存在しないか最小限の内容
- GitHubから来た開発者が「何のアプリか」「どう動くか」「どう使うか」を把握できない
- セルフホストしたいユーザー向けのセットアップ情報がない

**対応方針**
1. **概要セクション**: アプリ説明・スクリーンショット（利用イメージ）
2. **アーキテクチャセクション**: 構成図（フロントSPA → Netlify Functions → Gmail API → Google OAuth の流れ）
3. **セルフホスト手順**:
   - 前提条件（Netlify アカウント、Google Cloud Console の OAuth クライアント ID・シークレット、Gmail API 有効化）
   - リポジトリ fork → Netlify に接続 → 環境変数設定 → デプロイ
   - 必要な環境変数一覧（`GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `SESSION_SECRET` 等）
4. **機能一覧セクション**: 主要機能の説明
5. **ライセンス**: MIT

**確認方法**
- GitHub のリポジトリページで README が正しく表示されること
- セルフホスト手順に沿って別アカウントでデプロイできること（手順の網羅性確認）

---

### T008 · デモモード（ログインなしで試せる機能）

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 中 |
| **概要** | Google ログインなしでアプリの UI・機能を体験できるデモモードを追加する |

**背景**
- ランディングページから「試してみたい」ユーザーが Google ログインのハードルで離脱する
- デモモードがあれば機能・UX を事前に確認してからアカウント接続を判断できる

**調査内容**
- デモデータ: サンプルスレッド・送信者・ラベルを JSON でモック
- API モック: Netlify Functions を呼ばず、フロント側でモックレスポンスを返す
- デモ制限: 一括操作・プラグイン実行はデモ表示のみ（実際には送信しない）

**対応方針**
1. ランディングページに「デモを試す」ボタンを追加
2. `/demo` ルートを作成（またはクエリパラメータ `?demo=1`）
3. `apiFetch` をラップして、デモモード時はサンプルデータを返す mock 層を挿入
4. デモ中は常にバナーで「デモモードです。実際のメールには接続していません。」と表示
5. デモ終了・ログインへ誘導するボタンを常時表示

**確認方法**
- ランディングページの「デモを試す」ボタンを押すとログインなしでスレッド一覧が表示されること
- 一括操作を試みた場合は実際のAPIは呼ばれないこと
- デモモードバナーが常時表示されること

---

### T009 · 削除確認ダイアログの抜け漏れ修正

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 高 |
| **概要** | 削除ボタンを押した時に確認ダイアログが表示されない箇所がある |

**調査内容**
- BulkActionBar（一括操作バー）には確認ダイアログあり
- ThreadView の個別メッセージ・スレッド全体操作の削除ボタンにも確認が必要か確認する
- 全ての削除トリガー箇所を洗い出す

**対応方針**
- 確認が抜けている箇所を全て特定して統一的に確認ダイアログを追加

**確認方法**
- 全ての削除ボタンを押して確認ダイアログが表示されること

---

### T010 · アクション実行中メールへのマーク表示

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 中 |
| **概要** | バックグラウンドでアクション実行中のスレッドに、一覧・詳細両方でマークを表示する |

**調査内容**
- `taskQueueStore` の `tasks` から処理中スレッドID一覧を取得できる
- ThreadListItem と ThreadView でそのIDセットを参照してマーク表示する

**対応方針**
- store に `processingThreadIds` computed を追加（全 active タスクの threadIds を flatten）
- ThreadListItem でそのIDセットを参照して対象スレッドにスピナー/アイコン表示
- ThreadView のヘッダー部分にも同様の表示を追加

**確認方法**
- 一括処理実行中に一覧に戻ると該当スレッドにマークが表示されること
- 処理完了後にマークが消えること

---

### T011 · 差出人フィルタ時のスマホビューレイアウト崩れ

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 高 |
| **概要** | 差出人検索・フィルタ後のリロード等で差出人リストとメール一覧が両方表示され、一覧が画面下にずれる |

**調査内容**
- InboxView のルーティング・表示条件を確認する
- `sender` クエリパラメータがある場合に差出人リストが表示される条件を調査
- `SenderList` と `ThreadList` の表示切り替えロジックを確認

**対応方針**
- 差出人リストとメール一覧の表示条件を修正し、同時に表示されないようにする

**確認方法**
- 差出人名をタップ → sender フィルタ適用状態でリロード → 差出人リストだけ or メール一覧だけが表示されること
- スマホ幅（375px）でレイアウトが崩れないこと

---

### T012 · iPad PWA でウィンドウコントロールボタンがヘッダーと重畳する

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 中 |
| **概要** | iPad の PWA スタンドアロンモードでウィンドウを画面最上部に移動すると、macOS のトラフィックライトボタン（赤黄緑）がアプリヘッダーの左上コンテンツに重なる |

**調査内容**
- ウィンドウが画面上部に密着するとステータスバーと干渉し、OS がボタンをアプリ描画領域内に降ろしてくる
- `env(safe-area-inset-top)` は iOS のノッチ・ステータスバー用で既に対応済み
- macOS/iPadOS PWA のウィンドウコントロール領域は `env(titlebar-area-x)` / `env(titlebar-area-width)` / `env(titlebar-area-height)` で取得可能（Window Controls Overlay API）
- ただし WCO は `display_override: window-controls-overlay` をマニフェストに追加する必要あり

**対応方針案**
- **案A（シンプル）**: ヘッダーの左端に `padding-left: max(env(titlebar-area-x, 0px) + env(titlebar-area-width, 0px), <既存値>)` を追加し、ボタン占有幅分だけコンテンツを右にずらす
- **案B（WCO利用）**: マニフェストに `display_override: window-controls-overlay` を追加、CSS で `app-region: drag` を設定してヘッダーをドラッグハンドルに、コンテンツエリアを `titlebar-area-*` で制御する
- まずは案A（マニフェスト変更なし）で試し、不十分なら案B を検討

**確認方法**
- iPad PWA をホーム画面から起動 → ウィンドウを画面最上部に移動 → ヘッダー左上コンテンツとトラフィックライトが重ならないこと

---

### T013 · タスクキューバナーの折りたたみ対応

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 中 |
| **概要** | タスクが多い場合にキューバナーがメイン領域を圧迫する。右下フローティングアイコンでバナーをトグルできるようにする |

**調査内容**
- 現状: `TaskQueueBanner.vue` がタスク数分だけ縦に伸び、メインコンテンツが見えなくなる
- 対象: `apps/web/src/components/TaskQueueBanner.vue`

**対応方針**
- デフォルトは展開状態、右下フローティングボタン（アクティブ件数バッジ付き）でトグル
- 折りたたみ時はバナー非表示、フローティングボタンだけ残る
- `collapsed` の状態は `ref` で管理（ページをまたいでも維持したい場合は localStorage）

**確認方法**
- タスクが複数ある状態でフローティングボタンをタップするとバナーが折りたたまれること
- 再タップで展開されること
- バナー非表示時もフローティングボタンにアクティブ件数が表示されること

---

### T003 · invalid_grant 時の再ログイン誘導

| 項目 | 内容 |
|------|------|
| **ステータス** | ✅ 完了 |
| **優先度** | 高 |
| **概要** | `invalid_grant` 発生時に500ではなく401を返し、フロントがログイン画面に誘導するようにする |

**調査結果**
- リフレッシュトークン自体を別トークンで更新する手段はOAuth 2.0仕様上存在しない
- `invalid_grant` = リフレッシュトークンの死亡 → ユーザーの再ログインが必須
- アプリを定期的に使っていれば `getValidToken()` のリフレッシュ呼び出しが非アクティブタイマーをリセットするため、普通の利用では6ヶ月失効は起きにくい
- **本当の問題**: `invalid_grant` 時も `500` を返しており、フロントが再ログインを促せない
  - `packages/functions/src/lib/google-auth.ts:56` の `refreshAccessToken()` が失敗時に `throw new Error('Token refresh failed: ...')` するだけ
  - 呼び出し元の各 gmail function がそのまま500を返す

**対応方針**
1. `token.ts` の `getValidToken()` で `tokens.refresh_token` が返ってきた場合にセッションへ保存する（現状は無視している）
   - Googleはリフレッシュ時にまれに新しいリフレッシュトークンを返す場合があり、それを使い続けることで寿命を延ばせる
2. `google-auth.ts` の `refreshAccessToken()` で `invalid_grant` を検知し専用エラーを throw
3. `token.ts` の `getValidToken()` でそれを catch して `401` 相当のエラーとして再 throw
4. 各 gmail function で catch して `{ statusCode: 401 }` を返す
5. フロント（`useThreads` 等）で 401 を検知 → `useAuthStore().logout()` → ログイン画面へ

**確認方法**
- Google アカウント設定でアプリのアクセスを手動で取り消した後にAPIを叩く
- `500` ではなく `401` が返り、アプリがログイン画面に遷移すること
