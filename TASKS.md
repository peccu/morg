# タスク管理

> 新規タスク追加・ステータス更新は `/task` スキルを使ってください。
> Lessons & Learnings は `.claude/rules/lessons.md` に記録します。

---

## 凡例

| ステータス | 意味 |
|-----------|------|
| 🔍 調査中 | 原因・方針を調査している |
| 📋 対応予定 | 方針決定済み、未着手 |
| 🚧 対応中 | 実装・修正中 |
| ✅ 完了 | 対応・確認済み |
| ⏸ 保留 | 優先度低・依存待ち |

---

## タスク一覧

### T001 · i18n対応

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 中 |
| **概要** | UI文字列の国際化対応 |

**調査内容**
- 現状: 日本語ハードコード
- 対象: vue-i18n または軽量な独自実装
- 調査すべき点: SSR不要・PWA対応・Viteプラグイン要否

**対応方針**
- TBD（調査後に記入）

**確認方法**
- ブラウザ言語設定を英語に切り替えて英語UIが表示されること

---

### T002 · エラーコピーボタンのフィードバックなし

| 項目 | 内容 |
|------|------|
| **ステータス** | 📋 対応予定 |
| **優先度** | 低 |
| **概要** | コピーボタン押下時に「コピーしました」等のフィードバックがない |

**調査内容**
- 対象ファイル: `apps/web/src/components/ThreadList.vue`, `apps/web/src/views/ThreadView.vue`
- `copyText()` が成功してもユーザーへの通知なし

**対応方針**
- `useToast()` の `show()` でトースト通知を出す
- または `copyText()` ヘルパー内でトースト呼び出し（共通化）

**確認方法**
- エラー表示状態でコピーボタンを押して「コピーしました」トーストが出ること

---

### T003 · トークンリフレッシュ自動化

| 項目 | 内容 |
|------|------|
| **ステータス** | 🔍 調査中 |
| **優先度** | 高 |
| **概要** | `invalid_grant` エラー（トークン期限切れ）が発生した際に自動リフレッシュして継続利用したい |

**エラー内容**
```
500: Token refresh failed: {
  "error": "invalid_grant",
  "error_description": "Token has been expired or revoked."
}
```

**調査内容**
- `invalid_grant` はリフレッシュトークン自体が失効した状態（≠ アクセストークン期限切れ）
- Googleの仕様: リフレッシュトークンは以下で失効する
  - 6ヶ月間未使用
  - ユーザーがアプリのアクセス権限を手動で取り消した
  - パスワード変更
  - アカウントの安全上の問題
- アクセストークン（1時間）の自動リフレッシュとは別問題
- 調査すべき点: netlify functionsのトークン管理実装を確認

**対応方針**
- TBD（実装確認後に記入）
- 方針候補A: `invalid_grant` 検知時にフロントへ再ログイン要求を返す（現実的）
- 方針候補B: 自動再認証フロー（実装複雑・セキュリティ考慮要）

**確認方法**
- リフレッシュトークンを手動で無効化してAPIを叩き、適切なエラー（401/ログイン誘導）が返ること
